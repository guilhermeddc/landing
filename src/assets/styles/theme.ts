import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#91D683",
    },
    secondary: {
      main: "#EF778D",
    },
    // background: {
    //   default: "#F4D079",
    // },
  },
});

export default theme;